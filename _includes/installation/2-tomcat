## Install Tomcat

	# Ubuntu repositories aren't used because their Tomcat is rather old.
	# You will probably need to adapt this link because it keeps changing.
	# See www-us.apache.org/dist/tomcat/tomcat-8
	wget www-us.apache.org/dist/tomcat/tomcat-8/v{{ site.latest-apache-tomcat }}/bin/apache-tomcat-{{ site.latest-apache-tomcat }}.tar.gz
	tar -xzvf apache-tomcat-{{ site.latest-apache-tomcat }}.tar.gz
	CATALINA_HOME=$(pwd)/apache-tomcat-{{ site.latest-apache-tomcat }}
	JRE_HOME=/usr/lib/jvm/java-8-oracle/jre

